{
  "name": "monorepo",
  "private": "true",
  "version": "1.0.0",
  "description": "This is the monorepo of my own website, https://saito.style",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lerna run dev --stream --parallel"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^8.1.2",
    "prettier": "^3.3.2",
    "prettier-plugin-jinja-template": "^1.4.0"
  }
}
